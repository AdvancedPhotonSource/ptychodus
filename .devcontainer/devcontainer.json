{
  "name": "ptychodus (CPU)",
  "build": {
    "context": "..",
    "dockerfile": "../containers/Dockerfile.cpu"
  },
  "postCreateCommand": "pip install -e .",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "charliermarsh.ruff"
      ]
    }
  }
}
